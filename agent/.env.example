# --- Model selection -------------------------------------------------------
# The agent auto-detects a provider from the API keys present below. To pin a
# specific provider/model, set these explicitly.
#
# MODEL_PROVIDER=anthropic
# MODEL_ID=claude-sonnet-4-5
#
# Reasoning/thinking effort: off | minimal | low | medium | high | xhigh
# THINKING_LEVEL=medium

# --- Provider API keys -----------------------------------------------------
# Provide at least one. Anthropic is preferred when both are present.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- GitHub (used by the upcoming gh access tools) -------------------------
# A fine-grained or classic PAT, or a token minted from a GitHub App.
# GITHUB_TOKEN=
